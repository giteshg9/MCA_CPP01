#include <iostream>
using namespace std;

int main()
{
    /*char op;
    float num1,num2;

    cout <<"Enter an oprator (+ - * /)" << endl;
    cin >> op;

    cout << "Enter two numbers : " << endl;
    cin >> num1 >> num2;

    switch (op)
    {
        case '+'
    }

    int marks;
    cout << "Enter your marks : ";
    cin >> marks;

    string result = (marks >= 33) ? "Pass":"Fail";
    cout << result << endl;*/

    int num = 0;

    (num == 0) ? "Zero" : ((num > 0)?"Posative" : "Negative");
    
}